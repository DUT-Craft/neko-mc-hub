# 项目协作规则

## 永久设计原则

- 全站组件语义均按 Naive UI 主题体系组织，优先使用 `NLayout`、`NMenu`、`NCard`、`NGrid`、`NButton`、`NTag`、`NBadge`、`NDataTable`、`NForm`、`NTabs`、`NModal`、`NDrawer`、`NImage`、`NTimeline`、`NList` 等组件语义。
- 视觉仍保持明亮、清爽、校园感的 Minecraft 猫娘社服务器大厅风格，不因为使用 Naive UI 而变成普通后台系统。
- 必要处可以使用 `gpt-image-2` 生成配图；配图必须服务信息展示，不能成为无文字、无功能、无说明的空模块。
- 同一级别的卡片、入口、列表项尽量保持一致的行数、高度、内边距和视觉重量，避免一排模块高低不齐。
- 不保留无用矩形、空图框、空白装饰块；如果使用图像，图像必须展示猫娘社、服务器大厅、活动、Wiki、申请等明确特征。
- 页面文案以简洁美观为主，只保留玩家完成操作所需的必要说明，删除冗余介绍和开发者视角说明。
- 奶油色、浅棕色区域内不要突然插入纯白大块；输入框、面板和表单底色应使用更柔和的浅奶油、浅草绿或低透明白。
- 布局不要让单侧出现大量空位；目录、筛选、标签等横向信息优先放到页面上方，主体内容尽量使用完整宽度。
- 全站避免变成“卡片拼图”；优先使用 Minecraft 空间隐喻组织页面，例如大厅地图、传送门选择器、告示牌墙、工具箱物品栏、照片展示廊、任务委托板和服务器状态终端。
- 卡片只作为局部信息容器；页面主结构应体现玩家在服务器大厅中选择去向、查看状态、领取任务、阅读公告的路径。
- 服务器信息按“服务器名 → 状态 → 在线人数 → 地址 → 操作”排序，说明文字只做辅助；一个信息块内最多一个主按钮。
- 交互反馈保持轻像素感：按钮 hover 向下压 2px，复制地址后短暂显示“已复制”，在线点轻微闪烁，卡片 hover 只改变边框。
